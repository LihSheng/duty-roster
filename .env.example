# Server Configuration
PORT=5000
NODE_ENV=development

# Email Configuration (for notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password
EMAIL_FROM=duty-roster@example.com

# WhatsApp Configuration (optional)
WHATSAPP_API_URL=https://api.whatsapp.com/v1/messages
WHATSAPP_API_TOKEN=your-whatsapp-api-token

# Database Configuration (optional - defaults to SQLite in database/duty_roster.db)
DB_PATH=database/duty_roster.db

# Notification Settings
REMINDER_DAYS=1  # Number of days before due date to send reminders

# Google Cloud Configuration (for deployment)
# GCP_PROJECT_ID=your-gcp-project-id
# GCP_REGION=us-central1